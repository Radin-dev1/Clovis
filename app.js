const file=document.querySelector('#file');const name=document.querySelector('#file-name');const copy=document.querySelector('#result-copy');const analyze=document.querySelector('#analyze');file.addEventListener('change',()=>{if(file.files[0])name.textContent=file.files[0].name});analyze.addEventListener('click',()=>{if(!file.files[0]){copy.textContent='Choose a scene first. Rex accepts GLB, GLTF, OBJ, and PLY files.';return}copy.textContent='Scene queued. Connect this button to your private Rex inference endpoint to return geometry and collision analysis.';analyze.textContent='Queued →'});
